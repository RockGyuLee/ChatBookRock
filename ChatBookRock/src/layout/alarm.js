import React, {useState, useEffect} from "react"
import {
    SafeAreaView,
    ScrollView,
    View,
    Text,
    TextInput,
    StatusBar,
    Button
} from 'react-native';

export function AlarmScreen({navigation}){
return (
    <View>
        <Text>{"알람 화면입니다."}</Text>
    </View>
    )
}