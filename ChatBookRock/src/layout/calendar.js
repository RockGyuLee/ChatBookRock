import React, {useState, useEffect} from "react"
import {
    SafeAreaView,
    ScrollView,
    View,
    Text,
    TextInput,
    StatusBar,
    Button
} from 'react-native';

export function CalendarScreen({navigation}){
return (
    <View>
        <Text>{"캘린더 화면입니다."}</Text>
    </View>
    )
}